apiVersion: v1
kind: Service
metadata:
  name: {{ include "nfo.fullname" . }}
  labels:
    {{- include "nfo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "nfo.selectorLabels" . | nindent 4 }}
